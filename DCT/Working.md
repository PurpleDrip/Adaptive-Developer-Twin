
Before the Working, we have to talk abt the core components of DCT.
1. The Extension on the IDE
2. The DCT Microservices Architecture Server
3. The Dashboard and Analytics

By default, the IDE is being monitored from 9am-5pm IST, which is the normal working office hours. But this metrics is altered by the admin. They can readjust the window of observation according to his needs.

System Architecture.

The IDE keeps sending the Raw Telemetry Data in Batches, these data are aggregated and is then gone through Fusion Process, which gives out a Meaningful Telemetry Data. These data are also saved to judge the user.

The Raw Telemetry Data Model -


